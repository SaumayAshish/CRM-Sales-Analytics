# Used by docker-compose.yml for the full-stack `docker-compose up` run.
# Copy to .env and set a real value before anything but local demo use --
# docker-compose.yml falls back to an insecure dev default if this is unset.
JWT_SECRET_KEY=change-me-to-a-random-64-char-string
